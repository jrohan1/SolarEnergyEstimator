import { StyleSheet } from 'react-native'

export const homeStyles = StyleSheet.create({
  button: {
    width: 160,
    marginTop: 30,
    marginBottom: 30,
  },
  headerStyle: {
    marginBottom: 50
  }
})
